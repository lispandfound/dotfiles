# -*- mode: snippet -*-
# name: pbs
# key: pbs
# --
#!/bin/bash
#PBS -N ${1:jobname}
#PBS -o $1.out
#PBS -e $1.err
#PBS -l walltime=${2:01:00:00}
#PBS -l select=1:ncpus=${3:4}:mem=${4:8gb}
#PBS -q ${5:work}
${6:#PBS -M ${7:user@example.com}
#PBS -m abe
}$0